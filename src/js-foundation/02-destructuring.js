//console.log( process.env);

const {USERDOMAIN, WebStorm} = process.env;
//console.table({USERDOMAIN, WebStorm});

const characteres = ['Mario', 'Luigi', 'Toad', 'Bowser'];
const [, , toad] = characteres;
//console.log(toad);